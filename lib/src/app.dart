import 'package:flutter/material.dart';
import 'package:tabs_widgets/src/MyTabs.dart';

class MyApp extends StatelessWidget  {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: MyTabs(),
    );
  }
}